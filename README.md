Scaffold new project using `git archive` command.

i.e.

```bash
seglo@Seans-MBP:~/source/scala-skeleton on master
$ mkdir ../libmagic-spike

seglo@Seans-MBP:~/source/scala-skeleton on master
$ git archive master | tar -x -C ../libmagic-spike
```